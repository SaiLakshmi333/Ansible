- name: error handling
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: check the users_output
      ansible.builtin.command: id roboshop
      register: user_output
      ignore_erros: true

    - name:  print the user_output
      ansible.builtin.debug:
       msg: "{{user_output}}"