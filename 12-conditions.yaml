- name: conditions
  hosts: local
  connection: local
  vars: 
    numberofsessions : 12
  tasks: 
    - name: check conditions
      ansible.builtin.debug:
        msg : number of sessions are {{numberofsessions}}
      when: numberofsessions > 15 
