- name: loops
  hosts: local
  connection : local
  tasks :
    - name: install packages
      ansible.builtin.package:
       name :{{item.name}}
       state :{{item.state}}
      loop:
       - {name: "nginx" , state: "absent" }
       - {name: "sql", state: "present" }